#!/bin/bash
# After running the cookiecutter config replace the appropriate variables
# replace eas.json with ready made one
# replace app.config.js with ready made one
# TODO replace this with a sed replace instead to always use the exact same config as BS for testing
