{
  "scripts": {
    "postinstall": "cd client && npm install",
    "heroku-postbuild": "cd client && npm run build"
  },
  "engines": {
    "node": "22.x",
    "npm": "10.x"
  },
  "cacheDirectories": [
    "client/node_modules",
    ".cache/ms-playwright"
  ],
  "dependencies": {}
}
