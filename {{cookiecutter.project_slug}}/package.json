{
  "scripts": {
    "postinstall": "cd client && bun install",
    "heroku-postbuild": "cd client && bun run build"
  },
  "cacheDirectories": [
    "client/node_modules",
    ".cache/Cypress"
  ],
  "dependencies": {}
}